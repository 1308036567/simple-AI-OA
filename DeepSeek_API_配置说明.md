# DeepSeek API 配置说明

## 概述

本系统已成功集成 DeepSeek API，替代了原有的本地大模型方案。DeepSeek API 提供了更稳定、高效的AI对话服务，无需本地部署大型模型文件。

## 配置步骤

### 1. 获取 DeepSeek API 密钥

1. 访问 [DeepSeek 官网](https://www.deepseek.com/)
2. 注册账号并登录
3. 进入 API 管理页面
4. 创建新的 API 密钥
5. 复制生成的 API 密钥

### 2. 配置系统

编辑 `backend/config.py` 文件，找到以下配置项：

```python
# AI模型配置
AI_MODEL_TYPE: str = "deepseek"  # 使用DeepSeek API
DEEPSEEK_API_KEY: str = "your-deepseek-api-key-here"  # 请替换为实际的API密钥
DEEPSEEK_BASE_URL: str = "https://api.deepseek.com"
DEEPSEEK_MODEL: str = "deepseek-chat"  # 模型名称
```

将 `DEEPSEEK_API_KEY` 的值替换为您获取的实际 API 密钥。

### 3. 环境变量配置（推荐）

为了安全起见，建议使用环境变量来配置 API 密钥：

1. 在项目根目录创建 `.env` 文件：

```bash
DEEPSEEK_API_KEY=your-actual-api-key-here
```

2. 修改 `config.py` 中的配置：

```python
DEEPSEEK_API_KEY: str = os.getenv("DEEPSEEK_API_KEY", "your-deepseek-api-key-here")
```

## 功能特性

### 1. 智能对话
- 支持自然语言对话
- 理解企业管理相关指令
- 提供专业的业务建议

### 2. 指令识别
系统能够识别并执行以下指令：
- **添加员工**："添加员工张三，身份证110101199001011234，手机13800138000"
- **创建工资表**："创建项目A工资表，张三8000元，李四7500元"
- **导出数据**："导出员工信息"、"下载工资表"
- **备份数据**："备份数据"

### 3. 上下文记忆
- 保持对话上下文
- 记住最近5轮对话历史
- 提供连贯的对话体验

## API 配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `DEEPSEEK_API_KEY` | - | API 密钥（必填） |
| `DEEPSEEK_BASE_URL` | `https://api.deepseek.com` | API 基础URL |
| `DEEPSEEK_MODEL` | `deepseek-chat` | 使用的模型名称 |
| `max_tokens` | `2048` | 最大生成token数 |
| `temperature` | `0.7` | 生成温度（0-1） |
| `timeout` | `30` | 请求超时时间（秒） |

## 成本优势

相比本地部署大模型，使用 DeepSeek API 具有以下优势：

1. **无需硬件投资**：不需要高性能GPU
2. **按需付费**：只为实际使用付费
3. **维护成本低**：无需管理模型文件和更新
4. **性能稳定**：专业的云服务保障
5. **快速响应**：无需本地模型加载时间

## 使用示例

### 1. 基础对话
```
用户：你好，我想了解一下系统功能
AI：您好！我是您的企业管理助手，可以帮您处理员工信息管理、工资表生成、支出记录等业务。您需要什么帮助吗？
```

### 2. 添加员工
```
用户：添加员工李明，身份证110101199001011234，手机13912345678
AI：正在为您添加员工信息：李明。我已经记录了身份证号和手机号，请问还需要添加银行卡信息吗？
```

### 3. 生成工资表
```
用户：创建2024年3月工资表，张三8000元，李四7500元
AI：正在为您创建2024年3月工资表，包含张三（8000元）和李四（7500元）的工资信息。
```

## 故障排查

### 1. API 密钥错误
**错误信息**："DeepSeek API密钥未配置"
**解决方案**：检查 `config.py` 中的 `DEEPSEEK_API_KEY` 配置

### 2. 网络连接问题
**错误信息**："API连接测试失败"
**解决方案**：
- 检查网络连接
- 确认防火墙设置
- 验证 API 基础URL

### 3. API 调用失败
**错误信息**："DeepSeek API调用失败"
**解决方案**：
- 检查 API 密钥是否有效
- 确认账户余额充足
- 查看 API 使用限制

## 安全建议

1. **保护 API 密钥**：
   - 不要在代码中硬编码 API 密钥
   - 使用环境变量或配置文件
   - 定期轮换 API 密钥

2. **访问控制**：
   - 限制 API 密钥的使用范围
   - 监控 API 使用情况
   - 设置使用限额

3. **数据安全**：
   - 不要发送敏感信息到 API
   - 对传输数据进行加密
   - 遵守数据保护法规

## 技术支持

如果在配置或使用过程中遇到问题，请：

1. 查看系统日志文件
2. 检查网络连接状态
3. 验证 API 配置信息
4. 联系技术支持团队

---

**注意**：请确保您的 DeepSeek API 账户有足够的余额，并了解相关的使用条款和定价政策。