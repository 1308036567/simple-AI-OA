# 小微企业大模型交互管理系统 - 模块解析文档

## 📋 目录

1. [系统架构概览](#系统架构概览)
2. [后端模块详解](#后端模块详解)
3. [前端模块详解](#前端模块详解)
4. [数据库设计](#数据库设计)
5. [API接口设计](#api接口设计)
6. [工具类模块](#工具类模块)
7. [配置管理](#配置管理)
8. [部署架构](#部署架构)

## 🏗 系统架构概览

```
小微企业大模型交互管理系统
├── backend/                    # 后端服务
│   ├── main.py                # 应用入口
│   ├── config/                # 配置模块
│   ├── models/                # 数据模型
│   ├── routers/               # 路由控制器
│   ├── schemas/               # 数据验证模式
│   ├── utils/                 # 工具类
│   └── requirements.txt       # 依赖包
├── frontend/                  # 前端应用
│   ├── index.html            # 主页面
│   ├── js/app.js             # 应用逻辑
│   └── css/style.css         # 样式文件
├── data/                     # 数据存储
│   ├── files/               # 文件存储
│   ├── backups/             # 备份文件
│   └── logs/                # 日志文件
└── docs/                    # 文档
```

### 技术架构

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   移动端浏览器    │    │   内网穿透(frp)   │    │   本地服务器      │
│   Vue3 + Vant   │◄──►│   HTTPS/WSS     │◄──►│   FastAPI       │
│   响应式设计      │    │   域名访问        │    │   Python 3.8+   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                                        │
                                                        ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   本地大模型      │    │   文件存储系统     │    │   SQLite/MySQL  │
│   ChatGLM-6B    │◄──►│   加密存储        │◄──►│   数据持久化      │
│   语音识别/合成   │    │   分类管理        │    │   事务支持       │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 🔧 后端模块详解

### 1. 应用入口模块 (main.py)

**功能**: 应用启动入口，配置FastAPI应用

**主要组件**:
- FastAPI应用实例创建
- 中间件配置（CORS、日志、异常处理）
- 路由注册
- 静态文件服务
- 应用生命周期管理

**关键代码结构**:
```python
app = FastAPI(
    title="小微企业管理系统",
    description="基于AI的企业管理解决方案",
    version="1.0.0"
)

# 中间件配置
app.add_middleware(CORSMiddleware, ...)
app.add_middleware(LoggingMiddleware, ...)

# 路由注册
app.include_router(employees_router, prefix="/api/employees")
app.include_router(files_router, prefix="/api/files")
app.include_router(accounts_router, prefix="/api/accounts")
app.include_router(ai_chat_router, prefix="/api/ai")
```

### 2. 配置模块 (config/)

#### 2.1 数据库配置 (database.py)
**功能**: 数据库连接和会话管理

**主要组件**:
- 数据库URL构建
- 连接池配置
- 异步/同步会话工厂
- 数据库健康检查

#### 2.2 应用设置 (settings.py)
**功能**: 应用全局配置管理

**配置项**:
```python
class Settings(BaseSettings):
    # 数据库配置
    DATABASE_URL: str = "sqlite:///./data/sme.db"
    
    # 安全配置
    ENCRYPTION_KEY: str
    SECRET_KEY: str
    
    # AI模型配置
    AI_MODEL_PATH: str = "./models/chatglm-6b"
    AI_MODEL_TYPE: str = "chatglm"
    
    # 文件存储配置
    FILE_STORAGE_PATH: str = "./data/files"
    MAX_FILE_SIZE: int = 100 * 1024 * 1024  # 100MB
    
    # 服务器配置
    HOST: str = "0.0.0.0"
    PORT: int = 8000
    DEBUG: bool = False
```

### 3. 数据模型模块 (models/)

#### 3.1 基础模型 (base.py)
**功能**: 定义所有模型的基类

**特性**:
- 统一的主键定义
- 创建/更新时间自动管理
- 软删除支持
- 序列化方法

#### 3.2 员工模型 (employee.py)
**功能**: 员工信息数据模型

**字段设计**:
```python
class Employee(Base):
    __tablename__ = "employees"
    
    id: int                    # 主键
    name: str                  # 姓名
    id_card: str              # 身份证号（加密）
    phone: str                # 手机号（加密）
    bank_card: str            # 银行卡号（加密）
    bank_name: str            # 开户行
    department: str           # 部门
    position: str             # 职位
    hire_date: datetime       # 入职日期
    is_archived: bool         # 是否归档
    created_at: datetime      # 创建时间
    updated_at: datetime      # 更新时间
```

#### 3.3 文件模型 (file.py)
**功能**: 文件记录数据模型

**字段设计**:
```python
class FileRecord(Base):
    __tablename__ = "file_records"
    
    id: int                   # 主键
    filename: str             # 文件名
    original_filename: str    # 原始文件名
    file_path: str           # 存储路径
    file_size: int           # 文件大小
    file_type: str           # 文件类型
    category: str            # 文件分类
    business_id: str         # 业务关联ID
    upload_date: datetime    # 上传时间
    uploaded_by: str         # 上传者
    description: str         # 文件描述
```

#### 3.4 账表模型 (account.py)
**功能**: 账表和记录数据模型

**主要模型**:
- `Account`: 账表主表
- `SalaryRecord`: 工资记录
- `ExpenseRecord`: 支出记录

### 4. 路由控制器模块 (routers/)

#### 4.1 员工管理路由 (employees.py)
**功能**: 员工相关API端点

**主要端点**:
```python
@router.get("/")                    # 获取员工列表
@router.post("/")                   # 创建员工
@router.get("/{employee_id}")       # 获取员工详情
@router.put("/{employee_id}")       # 更新员工信息
@router.delete("/{employee_id}")    # 删除员工
@router.post("/import")             # 批量导入
@router.get("/export")              # 导出员工数据
@router.put("/{employee_id}/archive") # 归档员工
```

#### 4.2 文件管理路由 (files.py)
**功能**: 文件相关API端点

**主要端点**:
```python
@router.get("/")                    # 获取文件列表
@router.post("/upload")             # 上传文件
@router.get("/{file_id}/download")  # 下载文件
@router.delete("/{file_id}")        # 删除文件
@router.put("/{file_id}")           # 更新文件信息
@router.get("/categories")          # 获取文件分类
```

#### 4.3 账表管理路由 (accounts.py)
**功能**: 账表相关API端点

**主要端点**:
```python
@router.get("/")                    # 获取账表列表
@router.post("/")                   # 创建账表
@router.get("/{account_id}")        # 获取账表详情
@router.post("/{account_id}/salary") # 添加工资记录
@router.post("/{account_id}/expense") # 添加支出记录
@router.get("/{account_id}/export")  # 导出账表
@router.get("/{account_id}/chart")   # 生成图表
@router.post("/batch-salary")       # 批量创建工资记录
@router.get("/{account_id}/stats")   # 获取统计信息
```

#### 4.4 AI交互路由 (ai_chat.py)
**功能**: AI对话和指令处理

**主要端点**:
```python
@router.post("/chat")               # 文本对话
@router.post("/voice")              # 语音对话
@router.get("/history")             # 获取对话历史
@router.delete("/history")          # 清除对话历史
@router.post("/execute")            # 执行AI指令
@router.get("/commands")            # 获取可用指令
@router.get("/model/status")        # 获取模型状态
@router.post("/model/reload")       # 重新加载模型
```

### 5. 数据验证模式 (schemas/)

#### 5.1 员工模式 (employee.py)
**功能**: 员工数据的请求/响应模型

**主要模式**:
```python
class EmployeeBase(BaseModel):
    name: str = Field(..., min_length=1, max_length=50)
    department: str = Field(..., max_length=100)
    position: str = Field(..., max_length=100)
    
class EmployeeCreate(EmployeeBase):
    id_card: str = Field(..., regex=r'^\d{17}[\dXx]$')
    phone: str = Field(..., regex=r'^1[3-9]\d{9}$')
    bank_card: str = Field(..., min_length=16, max_length=19)
    bank_name: str = Field(..., max_length=100)
    hire_date: date
    
class EmployeeResponse(EmployeeBase):
    id: int
    is_archived: bool
    created_at: datetime
    updated_at: datetime
```

#### 5.2 文件模式 (file.py)
**功能**: 文件数据的请求/响应模型

#### 5.3 账表模式 (account.py)
**功能**: 账表数据的请求/响应模型

#### 5.4 AI聊天模式 (ai_chat.py)
**功能**: AI交互的请求/响应模型

## 🎨 前端模块详解

### 1. 主页面 (index.html)
**功能**: 单页面应用的HTML结构

**主要组件**:
- Vue应用挂载点
- 外部库引入（Vue、Vant、Axios）
- 基础HTML结构
- 响应式meta标签
- PWA支持

### 2. 应用逻辑 (js/app.js)
**功能**: 前端应用的核心逻辑

**主要模块**:

#### 2.1 应用初始化
```javascript
const app = createApp({
    data() {
        return {
            currentPage: 'home',
            globalLoading: false,
            // ... 其他状态
        };
    },
    mounted() {
        this.initApp();
        this.initSpeechRecognition();
        this.loadHomeStats();
    }
});
```

#### 2.2 API通信模块
```javascript
const api = axios.create({
    baseURL: API_CONFIG.baseURL,
    timeout: 30000
});

// 请求拦截器
api.interceptors.request.use(config => {
    // 显示加载状态
    // 添加认证头
});

// 响应拦截器
api.interceptors.response.use(
    response => response,
    error => {
        // 统一错误处理
        showNotify({ type: 'danger', message });
    }
);
```

#### 2.3 页面管理模块
```javascript
methods: {
    navigateTo(page) {
        this.currentPage = page;
    },
    
    resetPageData(page) {
        // 重置页面数据
    },
    
    loadPageData(page) {
        // 加载页面数据
    }
}
```

#### 2.4 AI交互模块
```javascript
// 文本对话
async sendMessage() {
    const response = await api.post('/api/ai/chat', {
        message: this.inputMessage,
        session_id: this.currentSessionId
    });
},

// 语音识别
startRecording() {
    this.recognition.start();
},

// 语音合成
speakText(text) {
    const utterance = new SpeechSynthesisUtterance(text);
    speechSynthesis.speak(utterance);
}
```

### 3. 样式设计 (css/style.css)
**功能**: 响应式UI样式

**设计特点**:
- 移动端优先设计
- 深色模式支持
- 流畅的动画效果
- 触摸友好的交互
- 安全区域适配

**主要样式模块**:
```css
/* 全局样式重置 */
* { margin: 0; padding: 0; box-sizing: border-box; }

/* 应用容器 */
#app { height: 100vh; display: flex; flex-direction: column; }

/* 顶部导航 */
.top-nav { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); }

/* 主要内容区域 */
.main-content { flex: 1; overflow-y: auto; }

/* 底部导航 */
.bottom-nav { position: fixed; bottom: 0; }

/* 响应式设计 */
@media (max-width: 375px) { /* 小屏幕适配 */ }

/* 深色模式 */
@media (prefers-color-scheme: dark) { /* 深色主题 */ }
```

## 🗄 数据库设计

### 1. 表结构设计

#### 1.1 员工表 (employees)
```sql
CREATE TABLE employees (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(50) NOT NULL,
    id_card TEXT NOT NULL,              -- 加密存储
    phone TEXT NOT NULL,                -- 加密存储
    bank_card TEXT NOT NULL,            -- 加密存储
    bank_name VARCHAR(100) NOT NULL,
    department VARCHAR(100) NOT NULL,
    position VARCHAR(100) NOT NULL,
    hire_date DATE NOT NULL,
    is_archived BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 1.2 文件记录表 (file_records)
```sql
CREATE TABLE file_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    filename VARCHAR(255) NOT NULL,
    original_filename VARCHAR(255) NOT NULL,
    file_path TEXT NOT NULL,
    file_size INTEGER NOT NULL,
    file_type VARCHAR(50) NOT NULL,
    category VARCHAR(50) NOT NULL,
    business_id VARCHAR(100),
    upload_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    uploaded_by VARCHAR(100),
    description TEXT
);
```

#### 1.3 账表主表 (accounts)
```sql
CREATE TABLE accounts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    table_name VARCHAR(200) NOT NULL,
    table_type VARCHAR(50) NOT NULL,    -- 'salary' or 'expense'
    description TEXT,
    created_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(100) NOT NULL
);
```

#### 1.4 工资记录表 (salary_records)
```sql
CREATE TABLE salary_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id INTEGER NOT NULL,
    employee_name VARCHAR(50) NOT NULL,
    base_salary DECIMAL(10,2) NOT NULL,
    bonus DECIMAL(10,2) DEFAULT 0,
    deduction DECIMAL(10,2) DEFAULT 0,
    actual_salary DECIMAL(10,2) NOT NULL,
    pay_date DATE NOT NULL,
    remark TEXT,
    FOREIGN KEY (account_id) REFERENCES accounts(id)
);
```

#### 1.5 支出记录表 (expense_records)
```sql
CREATE TABLE expense_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id INTEGER NOT NULL,
    expense_type VARCHAR(100) NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    description TEXT,
    expense_date DATE NOT NULL,
    handler VARCHAR(100),
    remark TEXT,
    FOREIGN KEY (account_id) REFERENCES accounts(id)
);
```

### 2. 索引设计

```sql
-- 员工表索引
CREATE INDEX idx_employee_name ON employees(name);
CREATE INDEX idx_employee_department ON employees(department);
CREATE INDEX idx_employee_archived ON employees(is_archived);

-- 文件表索引
CREATE INDEX idx_file_category ON file_records(category);
CREATE INDEX idx_file_business_id ON file_records(business_id);

-- 账表索引
CREATE INDEX idx_account_type ON accounts(table_type);
CREATE INDEX idx_salary_account_id ON salary_records(account_id);
CREATE INDEX idx_expense_account_id ON expense_records(account_id);
```

### 3. 数据关系图

```
employees (员工表)
    ↓ (一对多)
salary_records (工资记录) → accounts (账表)
                              ↑
expense_records (支出记录) ────┘

file_records (文件记录)
    ↓ (business_id关联)
[employees/accounts/...] (业务表)

chat_sessions (聊天会话)
    ↓ (一对多)
chat_messages (聊天消息)

system_logs (系统日志)
backup_records (备份记录)
```

## 🔌 API接口设计

### 1. RESTful API规范

#### 1.1 URL设计规范
```
GET    /api/employees          # 获取员工列表
POST   /api/employees          # 创建员工
GET    /api/employees/{id}     # 获取员工详情
PUT    /api/employees/{id}     # 更新员工
DELETE /api/employees/{id}     # 删除员工

GET    /api/files              # 获取文件列表
POST   /api/files/upload       # 上传文件
GET    /api/files/{id}/download # 下载文件

POST   /api/ai/chat           # AI对话
POST   /api/ai/voice          # 语音交互
```

#### 1.2 请求/响应格式

**标准响应格式**:
```json
{
    "success": true,
    "data": { ... },
    "message": "操作成功",
    "timestamp": "2024-01-01T12:00:00Z"
}
```

**错误响应格式**:
```json
{
    "success": false,
    "error": {
        "code": "VALIDATION_ERROR",
        "message": "数据验证失败",
        "details": { ... }
    },
    "timestamp": "2024-01-01T12:00:00Z"
}
```

#### 1.3 分页响应格式
```json
{
    "success": true,
    "data": {
        "items": [ ... ],
        "total": 100,
        "page": 1,
        "size": 20,
        "pages": 5
    }
}
```

### 2. 认证与授权

#### 2.1 API密钥认证
```python
@router.get("/protected")
async def protected_endpoint(
    api_key: str = Depends(get_api_key)
):
    # 受保护的端点
    pass
```

#### 2.2 请求频率限制
```python
from slowapi import Limiter

limiter = Limiter(key_func=get_remote_address)

@router.post("/upload")
@limiter.limit("10/minute")
async def upload_file(request: Request):
    # 限制上传频率
    pass
```

## 🛠 工具类模块

### 1. 数据验证工具 (utils/validation.py)

**功能**: 提供各种数据验证方法

**主要方法**:
```python
class DataValidator:
    @staticmethod
    def validate_id_card(id_card: str) -> bool:
        """验证身份证号码"""
        
    @staticmethod
    def validate_phone(phone: str) -> bool:
        """验证手机号码"""
        
    @staticmethod
    def validate_bank_card(card_number: str) -> bool:
        """验证银行卡号（Luhn算法）"""
        
    def validate_employee_data(self, data: dict) -> ValidationResult:
        """验证员工数据"""
```

### 2. 加密工具 (utils/encryption.py)

**功能**: 提供数据加密解密服务

**主要类**:
```python
class EncryptionManager:
    def __init__(self, key: bytes = None):
        self.key = key or self._generate_key()
        
    def encrypt_data(self, data: str) -> str:
        """加密数据"""
        
    def decrypt_data(self, encrypted_data: str) -> str:
        """解密数据"""
        
class PasswordManager:
    @staticmethod
    def hash_password(password: str) -> str:
        """密码哈希"""
        
    @staticmethod
    def verify_password(password: str, hashed: str) -> bool:
        """验证密码"""
```

### 3. Excel处理工具 (utils/excel_handler.py)

**功能**: Excel文件读写和处理

**主要方法**:
```python
class ExcelHandler:
    def read_employee_excel(self, file_path: str) -> List[dict]:
        """读取员工Excel文件"""
        
    def export_employees_to_excel(self, employees: List[Employee]) -> BytesIO:
        """导出员工数据到Excel"""
        
    def export_salary_table(self, account: Account, records: List[SalaryRecord]) -> BytesIO:
        """导出工资表"""
        
    def validate_excel_structure(self, file_path: str) -> ValidationResult:
        """验证Excel文件结构"""
```

### 4. AI模型工具 (utils/ai_model.py)

**功能**: AI模型交互和管理

**主要类**:
```python
class AIModelManager:
    def __init__(self):
        self.model = None
        self.tokenizer = None
        
    def load_model(self) -> bool:
        """加载AI模型"""
        
    def chat(self, message: str, history: List = None) -> str:
        """对话交互"""
        
    def parse_instruction(self, instruction: str) -> dict:
        """解析指令"""
        
    def speech_to_text(self, audio_data: bytes) -> str:
        """语音转文字"""
        
    def text_to_speech(self, text: str) -> bytes:
        """文字转语音"""
```

### 5. 图表生成工具 (utils/chart_generator.py)

**功能**: 数据可视化图表生成

**主要方法**:
```python
class ChartGenerator:
    def generate_salary_chart(self, data: List[dict], chart_type: str) -> str:
        """生成工资图表"""
        
    def generate_expense_chart(self, data: List[dict], chart_type: str) -> str:
        """生成支出图表"""
        
    def generate_dashboard_charts(self, stats: dict) -> dict:
        """生成仪表盘图表"""
```

### 6. 备份工具 (utils/backup.py)

**功能**: 系统数据备份和恢复

**主要方法**:
```python
class BackupManager:
    def create_full_backup(self) -> str:
        """创建完整备份"""
        
    def restore_backup(self, backup_path: str) -> bool:
        """恢复备份"""
        
    def list_backups(self) -> List[dict]:
        """列出备份文件"""
        
    def cleanup_old_backups(self, retention_days: int = 30):
        """清理过期备份"""
```

### 7. 日志工具 (utils/logger.py)

**功能**: 统一日志管理

**主要类**:
```python
class LoggerManager:
    def __init__(self):
        self.setup_loggers()
        
    def get_logger(self, name: str) -> logging.Logger:
        """获取日志记录器"""
        
    def log_operation(self, operation: str, user: str, details: dict):
        """记录操作日志"""
        
    def log_api_request(self, request: Request, response: Response):
        """记录API请求日志"""
        
    def log_ai_interaction(self, user_input: str, ai_response: str):
        """记录AI交互日志"""
```

## ⚙️ 配置管理

### 1. 环境配置

#### 1.1 开发环境 (.env.development)
```bash
DEBUG=True
DATABASE_URL=sqlite:///./data/dev.db
LOG_LEVEL=DEBUG
AI_MODEL_PATH=./models/chatglm-6b-int4
```

#### 1.2 生产环境 (.env.production)
```bash
DEBUG=False
DATABASE_URL=mysql://user:password@localhost/sme_db
LOG_LEVEL=INFO
AI_MODEL_PATH=/opt/models/chatglm-6b
ENCRYPTION_KEY=your-production-key
```

### 2. 配置加载机制

```python
from pydantic import BaseSettings

class Settings(BaseSettings):
    class Config:
        env_file = ".env"
        env_file_encoding = "utf-8"
        case_sensitive = False
        
    # 配置字段定义
    database_url: str = "sqlite:///./data/sme.db"
    debug: bool = False
    
settings = Settings()
```

### 3. 配置验证

```python
from pydantic import validator

class Settings(BaseSettings):
    encryption_key: str
    
    @validator('encryption_key')
    def validate_encryption_key(cls, v):
        if len(v) < 32:
            raise ValueError('加密密钥长度不能少于32字符')
        return v
```

## 🚀 部署架构

### 1. 本地部署架构

```
┌─────────────────────────────────────────────────────────────┐
│                    本地服务器                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │   FastAPI   │  │   SQLite    │  │  File Store │          │
│  │   :8000     │  │   Database  │  │   ./data/   │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │  AI Model   │  │   Backup    │  │    Logs     │          │
│  │  ChatGLM    │  │   System    │  │  ./logs/    │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    内网穿透层                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │     frp     │  │    nginx    │  │   SSL/TLS   │          │
│  │   Client    │  │   Reverse   │  │   Certificate│          │
│  │             │  │    Proxy    │  │             │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    移动端访问                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │   手机浏览器  │  │   平板浏览器  │  │   PC浏览器   │          │
│  │   Vue3+Vant │  │   响应式UI   │  │   兼容访问   │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

### 2. Docker容器化部署

#### 2.1 Dockerfile
```dockerfile
FROM python:3.9-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# 安装Python依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . .

# 创建数据目录
RUN mkdir -p /app/data /app/logs

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["python", "main.py"]
```

#### 2.2 docker-compose.yml
```yaml
version: '3.8'

services:
  sme-backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./models:/app/models
    environment:
      - DATABASE_URL=sqlite:///./data/sme.db
      - DEBUG=False
    restart: unless-stopped
    
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - sme-backend
    restart: unless-stopped
```

### 3. 高可用部署架构

```
┌─────────────────────────────────────────────────────────────┐
│                    负载均衡层                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │   Nginx     │  │   HAProxy   │  │  Cloudflare │          │
│  │   LB        │  │   Failover  │  │     CDN     │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    应用服务层                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │  FastAPI    │  │  FastAPI    │  │  FastAPI    │          │
│  │  Instance1  │  │  Instance2  │  │  Instance3  │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据存储层                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │   MySQL     │  │    Redis    │  │  File Store │          │
│  │   Master    │  │    Cache    │  │   Cluster   │          │
│  │   Slave     │  │             │  │             │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

### 4. 监控和运维

#### 4.1 健康检查
```python
@app.get("/health")
async def health_check():
    return {
        "status": "healthy",
        "timestamp": datetime.now(),
        "version": "1.0.0",
        "database": await check_database_health(),
        "ai_model": await check_ai_model_health()
    }
```

#### 4.2 性能监控
```python
from prometheus_client import Counter, Histogram

REQUEST_COUNT = Counter('http_requests_total', 'Total HTTP requests')
REQUEST_LATENCY = Histogram('http_request_duration_seconds', 'HTTP request latency')

@app.middleware("http")
async def monitor_requests(request: Request, call_next):
    start_time = time.time()
    response = await call_next(request)
    REQUEST_COUNT.inc()
    REQUEST_LATENCY.observe(time.time() - start_time)
    return response
```

### 5. 安全配置

#### 5.1 HTTPS配置
```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512;
    
    location / {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

#### 5.2 防火墙配置
```bash
# 只开放必要端口
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable

# 限制SSH访问
sudo ufw limit ssh
```

---

本文档详细描述了小微企业大模型交互管理系统的各个模块设计和实现细节，为开发、部署和维护提供了全面的技术参考。