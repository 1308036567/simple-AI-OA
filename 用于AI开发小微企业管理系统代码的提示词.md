### 小微企业管理系统代码开发提示词

#### 一、核心开发目标

基于 B/S 架构开发一套适配小微企业的管理系统，聚焦手机端远程访问与本地大模型交互，实现员工信息管理、文件管理、账表管理等核心功能，无需 PC 端前端，所有操作通过手机端完成。

#### 二、必须实现的功能模块及技术要求

1.  **员工信息管理模块**

*   支持人工输入员工信息（姓名、身份证、电话、银行卡、开户行等核心字段）。

*   实现 Excel 批量导入功能，需处理字段及列名差异（如 “身份证”“身份证号” 统一映射），导入时进行字段校验（身份证号格式、手机号有效性等），并生成预览匹配结果供手动修正。

*   开发一键归档功能，可将指定员工信息标记为归档状态。

*   支持通过本地大模型交互实现批量导出（接收大模型解析的指令，导出员工信息文档）。

1.  **文件管理系统**

*   实现文件夹分类功能（如按 “员工档案”“项目支出凭证” 等分类），支持文件上传、存储及关联业务 ID 记录（便于备份和查找）。

1.  **账表管理模块**

*   支持员工工资表、项目工程支出表等多种账表的创建与存储。

*   集成本地大模型交互，可根据指令自动填充员工信息生成工资表（如根据 “项目 A 员工工资：张三 8000 元” 等指令生成 Excel），并能按需求将数据可视化（调用 pyecharts 生成图表）或生成财务报表。

1.  **本地大模型交互层**

*   对接本地大模型（如 ChatGLM-6B 量化版），实现语音转文字（集成 Vosk 语音识别）、指令解析（将 “添加员工张三，身份证 110...” 等语音指令转为结构化操作）、响应生成（返回执行结果或文档 / 报表）。

1.  **手机端远程访问支持**

*   实现远程访问机制（如基于 frp 的内网穿透），支持手机端通过域名访问本地服务器，配合 HTTPS 加密。

*   开发极简手机端前端（响应式 Web，基于 Vue3+Vant），聚焦语音输入按钮、大模型回复展示、文件下载等交互，无需复杂页面设计。

1.  **数据安全与备份**

*   敏感数据（身份证号、银行卡号）采用 AES 加密存储。

*   记录所有操作日志（操作人、时间、内容）。

*   实现自动备份（每日凌晨）+ 手动备份（接收大模型指令触发），备份文件保留 30 天历史版本。

#### 三、无需开发的内容

1.  无需设计 PC 端前端界面（包括任何电脑端的页面布局、交互组件）。

2.  无需实现除姓名、身份证、电话、银行卡、开户行之外的非核心员工字段的特殊处理。

3.  无需对接云端大模型，所有大模型交互基于本地部署（模型文件存储在服务器本地）。

4.  无需开发复杂的用户权限管理（系统仅老板和管理层使用，无需分级登录）。

#### 四、技术栈约束

*   后端框架：Python FastAPI（提供 API 接口）。

*   数据库：MySQL（存储员工信息、账表数据等）。

*   移动端前端：Vue3+Vant（仅开发手机端适配的轻量化界面）。

*   部署要求：系统需可部署在本地电脑，支持手机端远程访问，代码需包含远程访问配置示例（如 frp 客户端配置）。

#### 五、交付物要求

*   可运行的后端代码（含 API 接口、数据处理逻辑、大模型交互逻辑）。

*   手机端前端代码（响应式 Web 页面）。

*   数据库设计脚本（表结构、索引等）。

*   关键功能的测试用例（如 Excel 导入导出、大模型指令解析等）。

> （注：文档部分内容可能由 AI 生成）